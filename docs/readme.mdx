---
id: 'CodeSnippet'
slug: /
sidebar_label: 简介
---
export const Highlight = ({children, color}) => (
    <span
        style={{
            backgroundColor: color,
            borderRadius: '2px',
            color: '#fff',
            padding: '0.2rem',
        }}>
    {children}
  </span>
);

:::tip

Code Snippet 代码片段

:::

在日常的业务需求开发中,出现的最多的就是我们对数据的处理包装成我们想要的结构,而这类代码往往是比较单纯
因为它不是业务逻辑而是单纯的数据处理和转换,对于这类代码仅仅依靠 `Java` 基础API即可完成,但是这类代码
也是比较烦躁的,每次遇到都要去写一遍,为了更加省时,这里准备了一大批的<Highlight color="#25c2a0">代码片段</Highlight>
快拷贝复制拿去光明正大的偷懒吧。


```java title="main函数"

public static void main(String[]args){

}

```

> zhege
